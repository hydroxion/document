version: 2

jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      
      - run:
        command: ../install.sh

      - run:
        command: make
        command: make run

workflows:
  version: 2

  workflow:
    jobs:
      - shellcheck

      - build-job:
          requires:
            - shellcheck

          filters:
            branches:
              only: master